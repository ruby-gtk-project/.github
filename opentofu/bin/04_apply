#!/usr/bin/env bash
# 4/4: apply the plan (this is where the import blocks land in state), then
# throw the state away — it is rebuilt from GitHub by bin/import every run.
set -euo pipefail
cd "$(dirname "$0")/.."
tofu apply -input=false tfplan
rm -f tfplan terraform.tfstate terraform.tfstate.backup
